@model IEnumerable<Alex_Andersen.Models.License>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LicenseName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LicenseName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LicenseId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LicenseId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LicenseId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
